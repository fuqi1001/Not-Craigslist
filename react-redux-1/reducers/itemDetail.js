



export default (state = [], action) => {
    let newState ;
    switch (action.type) {
        default:
            /*$.ajax({
                url: '/getItem',
                method: 'get',
            }).done(data => {
                console.log(data);

                //window.location.href = "/";
            }).fail( xhr => {
                console.log(xhr);
            })*/
            return state;
    }
};